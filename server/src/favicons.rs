
/* This is currently just a placeholder for if/when I decide to add randomized favicons */
/*use std::fs;
use std::path::PathBuf;

fn get_icons() -> Box<[]> {
	
	let dir = PathBuf::from("./client/favicons");
	let Some(iter) = fs::read_dir(dir);
	
}*/

